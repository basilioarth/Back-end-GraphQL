# Resumo da Aula 1 - Introdução ao GraphQL e Primeiros Passos

## Apresentação do Curso e do Projeto
- Instrutor: **Felipe Borelli**
- O foco do curso é aprender GraphQL **na prática**, construindo uma aplicação completa do zero.
- Projeto que será construído: **Mindshare**  
  Uma plataforma onde equipes podem:
  - Propor ideias
  - Dar feedback/comentários
  - Votar nas melhores ideias
- O Mindshare será usado para demonstrar todos os conceitos de GraphQL aplicados em uma aplicação real (cadastro, autenticação, votos, comentários, consultas otimizadas etc.).

## O que é GraphQL?
- Criado pelo Facebook em 2012, aberto (open source) em 2015.
- Diferente do REST (que tem vários endpoints com respostas fixas), o GraphQL possui:
  - **Apenas um único endpoint**
  - O cliente decide exatamente quais dados quer receber
- Vantagens mostradas no contexto do projeto:
  - Evita ter que fazer múltiplas requisições (ex: `/users`, `/ideas`, `/comments`)
  - Payload mais limpo e otimizado para o front-end

## Configuração Inicial do Projeto

Comandos executados:

```bash
npm init -y
# Adiciona "type": "module" no package.json para usar ESM

npm install graphql @apollo/server
npm install -D typescript tsx @types/node

npx tsc --init
```

Configurações importantes no `tsconfig.json`:
- Habilitado `experimentalDecorators` e `emitDecoratorMetadata` (será usado mais adiante)

Estrutura inicial criada:
```
src/
  └── index.ts
```

## Criação do Primeiro Servidor GraphQL com Apollo Server (modo Standalone)

Código principal (`index.ts`):

```ts
import { ApolloServer } from '@apollo/server';
import { startStandaloneServer } from '@apollo/server/standalone';

const typeDefs = #graphql
  type Query {
    helloWorld: String
  }
#;

const resolvers = {
  Query: {
    helloWorld: () => 'Hello World!'
  }
};

const server = new ApolloServer({
  typeDefs,
  resolvers
});

const { url } = await startStandaloneServer(server, {
  listen: { port: 4000 }
});

console.log(`Servidor iniciado em ${url}`);
```

Script adicionado no `package.json`:
```json
"dev": "tsx watch src/index.ts"
```

Execução:
```bash
npm run dev
```

## Testando a Primeira Query
- Ao acessar `http://localhost:4000`, abre o **Apollo Sandbox** (Playground)
- Funciona como um Postman específico para GraphQL
- Documentação automática do schema aparece no lado esquerdo
- Primeira query executada com sucesso:

```graphql
query {
  helloWorld
}
```

Resultado:
```json
{
  "data": {
    "helloWorld": "Hello World!"
  }
}
```

## O que vem na próxima aula
- Trocar o servidor standalone por **Express**
- Estruturar melhor os **resolvers** e **mutations**
- Criar os primeiros **modelos** de dados
- Instalar e configurar **SQLite** como banco de dados
- Começar a implementar cadastros, login e persistência de dados

**Pronto! O primeiro servidor GraphQL já está no ar e funcionando com a query básica "helloWorld".**  
Bora para a próxima aula! 🚀